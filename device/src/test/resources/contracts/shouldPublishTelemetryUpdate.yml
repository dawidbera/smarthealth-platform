# Contract Test: Device Service - Publish Telemetry Update Event
# Tests that the device service publishes telemetry update events to RabbitMQ

description: "Publish device telemetry update event"
label: "trigger_telemetry_update"  # Test scenario label

# Trigger condition
input:
  triggeredBy: triggerTelemetryUpdate()

# Expected Message Output
outputMessage:
  sentTo: internal.exchange  # RabbitMQ exchange
  headers:
    amqp_routingKey: device.telemetry.update  # Message routing key
  body:  # Message payload with telemetry data
    serialNumber: "SN-CONTRACT-001"
    value: 85.0
    unit: "BPM"
    timestamp: "2026-01-18T10:00:00"
